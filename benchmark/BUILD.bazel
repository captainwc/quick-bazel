load("@rules_cc//cc:cc_binary.bzl", "cc_binary")

cc_binary(
    name = "benchmark_demo",
    srcs = ["benchmark_demo.cpp"],
    deps = [
        "@google_benchmark//:benchmark",
    ],
    linkopts=[]
)
